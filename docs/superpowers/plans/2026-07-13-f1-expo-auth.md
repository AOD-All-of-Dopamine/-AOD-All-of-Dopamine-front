# F1: Expo 앱 부트스트랩 + 인증 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> (이 프로젝트 사용자 선호: 메인 모델 직접 TDD, 인라인 실행)

**Goal:** `apps/mobile`에 Expo RN 앱을 생성해 모노레포·@aod/shared에 연결하고, SecureStore 기반 3상태 인증(hydration)과 로그인/회원가입 화면까지 완성한다. (스펙 F1-A/B/C)

**Architecture:** Expo Router 기본 템플릿 위에 진입 `_layout.tsx`에서 QueryClient+ApiProvider 주입(웹 main.tsx와 대칭). 토큰 저장소만 expo-secure-store로 교체. 인증은 `loading → authenticated | unauthenticated` 3상태 머신. 스펙: `docs/superpowers/specs/2026-07-11-mobile-app-monorepo-design.md` §5·§6

**Tech Stack:** Expo(최신 SDK, create-expo-app 기본 템플릿=Expo Router), expo-secure-store, @tanstack/react-query v5, NativeWind, jest-expo + @testing-library/react-native

## Global Constraints

- 작업 브랜치: `feature/f1-expo-bootstrap` — **F0 브랜치(`feature/f0-monorepo-shared`) 위에 스택**. F0 머지 전엔 main 머지 금지, Vercel 설정은 F0 머지 직전 수행.
- `apps/web`·`packages/shared`의 **기존 파일 무수정** (shared에 파일 추가는 허용, 기존 로직 변경 금지).
- shared의 플랫폼 감지 금지 규칙 유지 — SecureStore 참조는 apps/mobile에만.
- API 주소는 `EXPO_PUBLIC_API_BASE_URL`로 주입. 기본값(개발)은 실서비스 API `https://api.allofdophamin.com` — 네이티브 앱은 CORS 무관하므로 로컬 백엔드 없이 로그인 왕복 검증 가능. 로컬 백엔드 사용 시 Android 에뮬레이터는 `http://10.0.2.2:8080`.
- Android SDK 경로(이 머신): `%LOCALAPPDATA%\Android\Sdk` (ANDROID_HOME 미설정 — 세션별 주입), AVD: `Pixel_3a_API_34_extension_level_7_x86_64`.
- 각 태스크 게이트: `pnpm --filter @aod/mobile typecheck` (T3부터 `pnpm --filter @aod/mobile test`도) + 기존 F0 게이트 유지(`pnpm -r test` 그린).

---

### Task 1: Expo 앱 생성 + 모노레포 연결 (F1-A)

**Files:**
- Create: `apps/mobile/` (create-expo-app 기본 템플릿 — Expo Router 포함)
- Modify: `apps/mobile/package.json` (name → `@aod/mobile`, typecheck 스크립트)

**Interfaces:**
- Produces: 에뮬레이터에서 실행되는 기본 앱. 워크스페이스 패키지 `@aod/mobile`.

- [ ] **Step 1: 스택 브랜치 생성**

```bash
git checkout -b feature/f1-expo-bootstrap   # feature/f0-monorepo-shared 위에서
```

- [ ] **Step 2: Expo 앱 생성**

```bash
cd apps
pnpm create expo-app@latest mobile --template default
```

생성 후 `apps/mobile/package.json`: `"name": "@aod/mobile"`, scripts에 `"typecheck": "tsc --noEmit"` 추가. 루트에서 `pnpm install`로 워크스페이스 재링크.

- [ ] **Step 3: 상태 점검**

```bash
pnpm --filter @aod/mobile exec npx expo-doctor
pnpm --filter @aod/mobile typecheck
```

Expected: expo-doctor 통과(모노레포 자동 인식 — 커스텀 metro 설정 넣지 않음), typecheck 그린.

- [ ] **Step 4: 헤드리스 번들 검증 (에뮬레이터 전 단계)**

```bash
pnpm --filter @aod/mobile exec npx expo export --platform android
```

Expected: dist/ 번들 생성 성공 — Metro가 전체 모듈 그래프를 해석했다는 뜻.

- [ ] **Step 5: 에뮬레이터 기동 확인 (수동 게이트)**

```powershell
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
# 별도 창: 에뮬레이터 부팅 후
pnpm --filter @aod/mobile exec npx expo start --android
```

Expected: 템플릿 홈 화면 렌더.

- [ ] **Step 6: Commit** — `feat(mobile): Expo 앱 부트스트랩 (@aod/mobile, Expo Router 템플릿)`

---

### Task 2: shared 연결 + API 주입 배선 (F1-B ①)

**Files:**
- Create: `apps/mobile/.env` (`EXPO_PUBLIC_API_BASE_URL=https://api.allofdophamin.com`), `.env.example`
- Create: `apps/mobile/src/lib/tokenStorage.ts`, `apps/mobile/src/lib/api.ts`, `apps/mobile/src/lib/queryClient.ts`
- Modify: `apps/mobile/app/_layout.tsx` (Provider 배선)

**Interfaces:**
- Consumes: `@aod/shared/api`의 `createApiClients`·`createApis`, `@aod/shared/hooks`의 `ApiProvider`
- Produces:

```ts
// tokenStorage.ts — SecureStore 어댑터 (mobile 전용 플랫폼 의존 지점)
export const tokenStorage = {
  get(): Promise<string | null>;
  set(token: string): Promise<void>;
  clear(): Promise<void>;
};
// api.ts — 앱 전역 1회 조립
export const apis: Apis;
```

- [ ] **Step 1: 의존성 추가**

```bash
pnpm --filter @aod/mobile exec npx expo install expo-secure-store @react-native-community/netinfo
pnpm --filter @aod/mobile add "@aod/shared@workspace:*" @tanstack/react-query axios
```

(react-query·axios는 shared의 peer/실행 의존 — 모바일 앱이 직접 선언)

- [ ] **Step 2: tokenStorage 구현**

```ts
// apps/mobile/src/lib/tokenStorage.ts
import * as SecureStore from "expo-secure-store";

const TOKEN_KEY = "token";

export const tokenStorage = {
  get: () => SecureStore.getItemAsync(TOKEN_KEY),
  set: (token: string) => SecureStore.setItemAsync(TOKEN_KEY, token),
  clear: () => SecureStore.deleteItemAsync(TOKEN_KEY),
};
```

- [ ] **Step 3: API 조립 + QueryClient**

```ts
// apps/mobile/src/lib/api.ts
import { createApiClients, createApis } from "@aod/shared/api";
import { tokenStorage } from "./tokenStorage";

const clients = createApiClients({
  baseURL: process.env.EXPO_PUBLIC_API_BASE_URL!,
  getToken: () => tokenStorage.get(),
  isDev: __DEV__,
});

export const apis = createApis(clients);
```

```ts
// apps/mobile/src/lib/queryClient.ts — 웹 main.tsx와 동일 정책 + RN 연결
import { QueryClient, onlineManager, focusManager } from "@tanstack/react-query";
import NetInfo from "@react-native-community/netinfo";
import { AppState } from "react-native";

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5, // 5 minutes (웹과 동일)
      refetchOnWindowFocus: false,
    },
  },
});

onlineManager.setEventListener((setOnline) =>
  NetInfo.addEventListener((state) => setOnline(!!state.isConnected)),
);

AppState.addEventListener("change", (status) =>
  focusManager.setFocused(status === "active"),
);
```

- [ ] **Step 4: `_layout.tsx` 배선** — 템플릿 루트 레이아웃의 최상위를 감싼다:

```tsx
import { QueryClientProvider } from "@tanstack/react-query";
import { ApiProvider } from "@aod/shared/hooks";
import { apis } from "../src/lib/api";
import { queryClient } from "../src/lib/queryClient";
// ...기존 템플릿 import 유지

export default function RootLayout() {
  // ...기존 템플릿 로직 유지
  return (
    <QueryClientProvider client={queryClient}>
      <ApiProvider apis={apis}>
        {/* 기존 템플릿 Stack/Theme 구조 그대로 */}
      </ApiProvider>
    </QueryClientProvider>
  );
}
```

- [ ] **Step 5: 검증** — `pnpm --filter @aod/mobile typecheck && pnpm --filter @aod/mobile exec npx expo export --platform android` 그린 (shared가 Metro에서 해석되는지 확인).

- [ ] **Step 6: Commit** — `feat(mobile): @aod/shared 연결 — SecureStore 토큰 주입 + ApiProvider 배선`

---

### Task 3: 인증 3상태 hydration (F1-B ②, TDD)

**Files:**
- Create: `apps/mobile/src/auth/AuthContext.tsx`
- Test: `apps/mobile/src/auth/__tests__/AuthContext.test.tsx`
- Modify: `apps/mobile/package.json` (jest-expo 설정)

**Interfaces:**
- Produces:

```ts
export type AuthState =
  | { status: "loading" }
  | { status: "authenticated"; user: UserInfo }
  | { status: "unauthenticated" };
export const AuthProvider: React.FC<{ children: React.ReactNode }>;
export function useAuth(): {
  state: AuthState;
  login(username: string, password: string): Promise<void>;
  signup(username: string, email: string, password: string): Promise<void>;
  logout(): Promise<void>;
};
```

동작 규칙 (웹 AuthContext 이식 + 스펙 §5):
- 부팅 시 `loading` → 토큰 없음이면 `unauthenticated`, 있으면 `getCurrentUser()` 성공 시 `authenticated`.
- 401/403이면 토큰 삭제 후 `unauthenticated`, **일시 오류(네트워크/5xx)면 토큰 유지한 채 authenticated로 간주하지 않고 unauthenticated 전이하되 토큰은 남긴다** — 웹과 동일하게 "인증 오류만 로그아웃".
- `login()`은 응답의 token 저장 + 응답 기준 user 확정 (웹과 동일).

- [ ] **Step 1: jest-expo 셋업**

```bash
pnpm --filter @aod/mobile add -D jest jest-expo @testing-library/react-native @types/jest
```

`apps/mobile/package.json`에 추가:

```jsonc
"scripts": { "test": "jest" },
"jest": {
  "preset": "jest-expo",
  "transformIgnorePatterns": [
    "node_modules/(?!((jest-)?react-native|@react-native(-community)?|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@sentry/react-native|native-base|react-native-svg|@aod/.*)/)"
  ]
},
```

- [ ] **Step 2: 실패하는 테스트 작성** — SecureStore·apis 모킹으로 3상태 전이 검증:

```tsx
// apps/mobile/src/auth/__tests__/AuthContext.test.tsx
import React from "react";
import { renderHook, waitFor } from "@testing-library/react-native";
import { AuthProvider, useAuth } from "../AuthContext";

const mockGet = jest.fn();
const mockSet = jest.fn();
const mockClear = jest.fn();
jest.mock("../../lib/tokenStorage", () => ({
  tokenStorage: {
    get: (...a: unknown[]) => mockGet(...a),
    set: (...a: unknown[]) => mockSet(...a),
    clear: (...a: unknown[]) => mockClear(...a),
  },
}));

const mockGetCurrentUser = jest.fn();
const mockLogin = jest.fn();
jest.mock("../../lib/api", () => ({
  apis: {
    authApi: {
      getCurrentUser: (...a: unknown[]) => mockGetCurrentUser(...a),
      login: (...a: unknown[]) => mockLogin(...a),
      signup: jest.fn(),
    },
  },
}));

const wrapper = ({ children }: { children: React.ReactNode }) => (
  <AuthProvider>{children}</AuthProvider>
);

describe("AuthContext hydration", () => {
  beforeEach(() => jest.clearAllMocks());

  it("토큰 없음 → unauthenticated", async () => {
    mockGet.mockResolvedValue(null);
    const { result } = renderHook(() => useAuth(), { wrapper });
    expect(result.current.state.status).toBe("loading");
    await waitFor(() =>
      expect(result.current.state.status).toBe("unauthenticated"),
    );
    expect(mockGetCurrentUser).not.toHaveBeenCalled();
  });

  it("유효 토큰 → authenticated (user 복원)", async () => {
    mockGet.mockResolvedValue("tok");
    mockGetCurrentUser.mockResolvedValue({ userId: 1, username: "u", email: "e" });
    const { result } = renderHook(() => useAuth(), { wrapper });
    await waitFor(() =>
      expect(result.current.state.status).toBe("authenticated"),
    );
  });

  it("401 → 토큰 삭제 + unauthenticated", async () => {
    mockGet.mockResolvedValue("expired");
    mockGetCurrentUser.mockRejectedValue({ response: { status: 401 } });
    const { result } = renderHook(() => useAuth(), { wrapper });
    await waitFor(() =>
      expect(result.current.state.status).toBe("unauthenticated"),
    );
    expect(mockClear).toHaveBeenCalled();
  });

  it("일시 오류(5xx) → unauthenticated지만 토큰은 유지", async () => {
    mockGet.mockResolvedValue("tok");
    mockGetCurrentUser.mockRejectedValue({ response: { status: 500 } });
    const { result } = renderHook(() => useAuth(), { wrapper });
    await waitFor(() =>
      expect(result.current.state.status).toBe("unauthenticated"),
    );
    expect(mockClear).not.toHaveBeenCalled();
  });

  it("login 성공 → 토큰 저장 + authenticated", async () => {
    mockGet.mockResolvedValue(null);
    mockLogin.mockResolvedValue({ token: "new", userId: 2, username: "me", message: "" });
    const { result } = renderHook(() => useAuth(), { wrapper });
    await waitFor(() =>
      expect(result.current.state.status).toBe("unauthenticated"),
    );
    await result.current.login("me", "pw");
    expect(mockSet).toHaveBeenCalledWith("new");
    await waitFor(() =>
      expect(result.current.state.status).toBe("authenticated"),
    );
  });
});
```

- [ ] **Step 3: 실패 확인** — `pnpm --filter @aod/mobile test` → FAIL (AuthContext 없음)

- [ ] **Step 4: AuthContext 구현**

```tsx
// apps/mobile/src/auth/AuthContext.tsx
import React, { createContext, useContext, useState, useEffect, useCallback } from "react";
import type { UserInfo } from "@aod/shared/api";
import { tokenStorage } from "../lib/tokenStorage";
import { apis } from "../lib/api";

export type AuthState =
  | { status: "loading" }
  | { status: "authenticated"; user: UserInfo }
  | { status: "unauthenticated" };

interface AuthContextType {
  state: AuthState;
  login: (username: string, password: string) => Promise<void>;
  signup: (username: string, email: string, password: string) => Promise<void>;
  logout: () => Promise<void>;
}

const AuthContext = createContext<AuthContextType | undefined>(undefined);

export const AuthProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [state, setState] = useState<AuthState>({ status: "loading" });

  const restore = useCallback(async () => {
    const token = await tokenStorage.get();
    if (!token) {
      setState({ status: "unauthenticated" });
      return;
    }
    try {
      const user = await apis.authApi.getCurrentUser();
      setState({ status: "authenticated", user });
    } catch (error) {
      const status = (error as { response?: { status?: number } })?.response?.status;
      if (status === 401 || status === 403) {
        await tokenStorage.clear(); // 인증 자체가 잘못된 경우만 로그아웃 (웹과 동일)
      }
      setState({ status: "unauthenticated" });
    }
  }, []);

  useEffect(() => {
    restore();
  }, [restore]);

  const login = async (username: string, password: string) => {
    const res = await apis.authApi.login({ username, password });
    await tokenStorage.set(res.token);
    setState({
      status: "authenticated",
      user: { userId: res.userId, username: res.username, email: "" },
    });
  };

  const signup = async (username: string, email: string, password: string) => {
    await apis.authApi.signup({ username, email, password });
  };

  const logout = async () => {
    await tokenStorage.clear();
    setState({ status: "unauthenticated" });
  };

  return (
    <AuthContext.Provider value={{ state, login, signup, logout }}>
      {children}
    </AuthContext.Provider>
  );
};

export function useAuth(): AuthContextType {
  const ctx = useContext(AuthContext);
  if (ctx === undefined) throw new Error("useAuth must be used within an AuthProvider");
  return ctx;
}
```

`_layout.tsx`의 ApiProvider 안쪽에 `<AuthProvider>` 추가.

- [ ] **Step 5: 통과 확인** — `pnpm --filter @aod/mobile test` → 5 PASS. typecheck 그린.

- [ ] **Step 6: Commit** — `feat(mobile): 인증 3상태 hydration (SecureStore) — jest-expo 테스트 5케이스`

---

### Task 4: 로그인·회원가입 화면 (F1-C)

**Files:**
- Create: `apps/mobile/app/(auth)/_layout.tsx`, `(auth)/login.tsx`, `(auth)/signup.tsx`
- Modify: `apps/mobile/app/_layout.tsx` (auth 게이트: loading 시 스플래시 유지)

**동작 (웹 login/signup 페이지와 동일한 API 흐름):**
- 로그인: username+password → `useAuth().login` → 성공 시 홈(탭)으로 replace, 실패 시 에러 텍스트 (웹과 동일 메시지 "로그인에 실패했습니다." 계열).
- 회원가입: username+email+password → `signup` → 성공 시 로그인 화면으로 이동.
- 스타일: NativeWind 기본 세팅 후 최소 스타일(입력 2~3개, 버튼, 에러 텍스트). 픽셀 폴리싱은 F2+.

- [ ] **Step 1: NativeWind 셋업** — 공식 설치 절차(nativewind, tailwindcss, babel preset, metro/global.css). Expo SDK 버전에 맞는 nativewind 버전 사용.

- [ ] **Step 2: 화면 구현** — `useAuth()` 사용, `router.replace("/")` 이동. 필드 상태는 `useState`, 제출 중 disabled.

- [ ] **Step 3: 수동 게이트 (에뮬레이터)** — 실서비스 API 대상 로그인 왕복: 로그인 → 앱 재시작(hydration으로 세션 유지) → 로그아웃. 회원가입은 테스트 계정 1회.

- [ ] **Step 4: 검증 + Commit** — typecheck·test·export 그린 → `feat(mobile): 로그인·회원가입 화면 (Expo Router (auth) 그룹)`

---

### Task 5: 마무리

- [ ] `pnpm -r test && pnpm -r --if-present typecheck && pnpm build` 전체 그린 (웹 회귀 없음 확인)
- [ ] `git push -u origin feature/f1-expo-bootstrap`
- [ ] 계획 체크박스 갱신 + 다음 단계(F2 탭+홈) 기록

## Self-Review

- 스펙 F1-A/B/C ↔ Task 1/2·3/4 커버. 테스트 우선순위(스펙 §9)의 3순위(hydration) 반영, 1·2순위는 F0에서 완료.
- 타입 일관성: `tokenStorage`(T2) ← AuthContext(T3) 모킹 경로 일치, `apis`(T2) ← 화면(T4).
- 유의: create-expo-app 템플릿 구조는 SDK 버전에 따라 다름 — _layout 배선은 "기존 템플릿 구조를 감싼다"가 원칙, 템플릿 예제 화면 정리는 F2에서. jest-expo transformIgnorePatterns에 `@aod/.*` 포함 필수(워크스페이스 TS 소스 변환).
